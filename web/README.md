# NeverDie — web preview

Static design showcase served by Vercel. `index.html` is the flagship
"Midnight & Candlelight" app preview (all screens + design system).
The product itself is the Expo React Native app under `apps/mobile/`.
